import 'package:flutter/material.dart';

class PresetPage extends StatelessWidget {
  const PresetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Preset Page'),
    );
  }
}
